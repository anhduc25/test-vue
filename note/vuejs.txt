1.
const app = Vue.createApp({
	template:"",
	data(){
		các thuộc tính object
	},
	methods:{
		danh sách các hàm
	},
	computed:{
		danh sách hàm
	}
})
app.mount(id or class để xác định đối tượng)

2. gọi data: `{{}}`

3. v-on = @

4. v:if : hiện nếu ... là đúng

5. @mouseover 

6. @mouseleave 

7. @dblclick 

8. @mousemove 

9.v-bind: truyền dữ liệu vào 1 thuộc tính ví dụ như src (đậy đủ v-bind:src tắt thì :src )

10. cart:isCart cấu trúc truyền class cart nếu isCart true

11. v-for= "product in products" cấu trúc duyệt 

12. filter(): vòng lặp for duyeẹt nx phần tử thỏa mãn điều kiện rồi trả về danh sách cần tìm

13. Event modifier: thêm các kiểu của sự kiện

14. v-model: thao tác cho input binding 2 chiều